# SPI Stack - Bare ingress profile
#
# Empty: the bare profile deploys no cert-manager or Gateway, so Flux selects
# this tree for bare regardless of the requested ingress mode.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
