# Helm sources used by more than one component (bitnami serves Redis and
# ExternalDNS). Shipping one with either consumer would let the other's
# inventory prune it.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - bitnami.yaml
